<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name="com.dowpro.marketapp.deeplink.DeepLinkActivity"
            android:exported="true">
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data
                    android:host="${deeplinkUrl}"
                    android:pathPrefix="/${brandName}/sentiments"
                    android:scheme="https" />

                <data
                    android:host="${deeplinkUrl}"
                    android:pathPrefix="/${brandName}/stock-details"
                    android:scheme="https" />

                <data
                    android:host="${deeplinkUrl}"
                    android:pathPrefix="/${brandName}/pokemons"
                    android:scheme="https" />

                <data
                    android:host="${deeplinkUrl}"
                    android:pathPrefix="/${brandName}/red"
                    android:scheme="https" />
            </intent-filter>
        </activity>

    </application>

</manifest>